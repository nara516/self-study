def solution(s):

    idx = int(len(s)/2)
    if len(s) % 2 == 0:
        answer = s[idx:idx+2]        
    elif len(s) % 2 == 1:
        answer = s[idx]   
        
    return answer
    
    
    
    
    """
    def string_middle(str):

    return str[(len(str)-1)//2:len(str)//2+1]
    """
